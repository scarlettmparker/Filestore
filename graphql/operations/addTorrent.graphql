mutation addTorrent($input: AddTorrentInput!) {
  filestoreMutations {
    addTorrent(input: $input) {
      id
      bucket
      targetKeyPath
      status
      progress
      magnetDetail {
        displayName
      }
    }
  }
}
