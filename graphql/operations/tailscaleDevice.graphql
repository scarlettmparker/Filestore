query tailscaleDevice($id: ID!) {
  gaiaQueries {
    tailscaleDevice(id: $id) {
      id
      headscaleId
      name
      ipv4
      status
      online
      expiredAt
      lastSeen
      createdAt
      updatedAt
    }
  }
}
