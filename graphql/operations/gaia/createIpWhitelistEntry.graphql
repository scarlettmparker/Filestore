mutation createIpWhitelistEntry($input: IpWhitelistEntryInput!) {
  gaiaMutations {
    createIpWhitelistEntry(input: $input) {
      ... on QuerySuccess {
        __typename
        message
        id
      }
      ... on StandardError {
        __typename
        message
      }
    }
  }
}
