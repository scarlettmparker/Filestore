import { Skeleton } from "@sun/components";

/**
 * Skeleton for the IP config detail panel while data loads.
 */
const IpDetailSkeleton = () => (
  <Skeleton style={{ width: "100%", height: "12rem" }} />
);

export default IpDetailSkeleton;
